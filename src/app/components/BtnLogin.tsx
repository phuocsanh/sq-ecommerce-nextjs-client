"use client";
import { Button } from "@/components/ui/button";
import React from "react";

function BtnLogin() {
  return (
    <Button onClick={() => {}} title="Click">
      Click
    </Button>
  );
}

export default BtnLogin;
